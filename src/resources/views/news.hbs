<div
    class="mt-4"
    style="
    margin: 20px;
"
>
    <div class="row">
        {{#each movie}}
            <div class="card" style="width: 20rem;">
                <img
                    class="card-img-top"
                    src={{this.backdrop_path}}
                    alt={{this.title}}
                />
                <div class="card-body">
                    <h5 class="card-title">{{this.original_title}}</h5>
                    <p class="card-text">{{this.overview}}</p>
                    <a href="/movie/{{this._id}}" class="btn btn-primary">Chi
                        tiết</a>
                </div>
            </div>
        {{/each}}
    </div>
</div>