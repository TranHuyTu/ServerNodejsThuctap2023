require("dotenv").config();
const ACCESS_TOKEN = process.env.ACCESS_TOKEN;
const TOKEN_TIME_LITE = process.env.TOKEN_TIME_LITE;

module.exports = {
    ACCESS_TOKEN: ACCESS_TOKEN,
    TOKEN_TIME_LITE: TOKEN_TIME_LITE,
};
